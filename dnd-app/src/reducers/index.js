
const initialState = {
  isLoading: false,
  monsters: null,
  error: 'TEST ERROR'
}

export const reducer = (state = initialState, action) => {
  switch(action.type){

    default:
      return state
  }
}